import { useState } from 'react';
const allowed = ['image/png', 'image/jpeg', 'image/webp'];
export function ImageUploader({ label, value, onChange }: { label: string; value: string; onChange: (url: string) => void }) { const [error, setError] = useState(''); const select = (file?: File) => { if (!file) return; if (!allowed.includes(file.type)) return setError('PNG / JPEG / WEBP ONLY'); if (file.size > 5 * 1024 * 1024) return setError('MAX FILE SIZE IS 5MB'); setError(''); const reader = new FileReader(); reader.onload = () => onChange(String(reader.result)); reader.readAsDataURL(file); }; return <label className="image-uploader"><span>{label}</span>{value && <img src={value} alt="preview" />}<input type="file" accept="image/png,image/jpeg,image/webp" onChange={e => select(e.target.files?.[0])} /><div><button type="button" onClick={() => onChange('')}>REMOVE</button></div>{error && <em>{error}</em>}</label>; }
