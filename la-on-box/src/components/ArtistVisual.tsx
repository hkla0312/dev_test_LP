import type React from 'react'; import type { BroadcastSlot } from '../types';
const fallback = (name: string) => `data:image/svg+xml,${encodeURIComponent(`<svg xmlns="http://www.w3.org/2000/svg" width="800" height="500"><rect width="100%" height="100%" fill="#111923"/><text x="50%" y="50%" fill="#dff" font-size="44" font-family="monospace" text-anchor="middle">${name}</text></svg>`)}`;
export function ArtistVisual({ slot, children }: { slot: BroadcastSlot; children?: React.ReactNode }) { const bg = slot.backgroundImageUrl || fallback('LA_ON-BOX AUDIO'); const thumb = slot.thumbnailUrl || fallback(slot.artistName); return <section className="artist-visual" style={{ backgroundImage: `linear-gradient(#03070bbd,#03070bd9),url(${bg})` }}><img className="artist-thumb" src={thumb} alt={`${slot.artistName} thumbnail`} onError={e => { e.currentTarget.src = fallback(slot.artistName); }} /><div className="danmaku-zone">{children}</div></section>; }
