import type { BroadcastSlot } from '../types';
const format = (seconds: number) => `${String(Math.max(0, Math.floor(seconds / 60))).padStart(2, '0')}:${String(Math.max(0, seconds % 60)).padStart(2, '0')}`;
export function StreamTimer({ slot, now }: { slot: BroadcastSlot; now: number }) { return <div className="timer"><small>TIME REMAINING</small><b>{format((new Date(slot.endsAt).getTime() - now) / 1000)}</b><span>{new Date(slot.startsAt).toLocaleTimeString('ja-JP', { hour: '2-digit', minute: '2-digit' })} — {new Date(slot.endsAt).toLocaleTimeString('ja-JP', { hour: '2-digit', minute: '2-digit' })}</span></div>; }
export function NextArtistCountdown({ slot, next, now }: { slot: BroadcastSlot; next: BroadcastSlot; now: number }) { const remain = (new Date(slot.endsAt).getTime() - now) / 1000, threshold = import.meta.env.VITE_LA_ON_BOX_DEBUG_MODE === 'true' ? 60 : 300; return remain <= threshold && remain > 0 ? <aside className="next-countdown"><small>NEXT CONNECTION</small><b>{format(remain)}</b><span>NEXT: {next.artistName}</span></aside> : null; }
