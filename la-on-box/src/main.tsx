import React from 'react'; import ReactDOM from 'react-dom/client'; import { BrowserRouter, Navigate, Route, Routes } from 'react-router-dom'; import { Home, UserLogin, BroadcasterLogin } from './pages/LoginPages'; import { Watch } from './pages/Watch'; import { Broadcast } from './pages/Broadcast'; import { broadcasterAuth } from './services/auth'; import './styles/app.css'; import './styles/app-footer.css';
const MemberGuard = ({ children }: { children: React.ReactNode }) => <>{children}</>;
const BroadcasterGuard = ({ children }: { children: React.ReactNode }) => broadcasterAuth.get() ? <>{children}</> : <Navigate to="/broadcaster-login" replace/>;
ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><BrowserRouter basename={import.meta.env.BASE_URL}><Routes><Route path="/" element={<Home/>}/><Route path="/user-login" element={<UserLogin/>}/><Route path="/broadcaster-login" element={<BroadcasterLogin/>}/><Route path="/watch" element={<MemberGuard><Watch/></MemberGuard>}/><Route path="/broadcast" element={<BroadcasterGuard><Broadcast/></BroadcasterGuard>}/><Route path="*" element={<Navigate to="/" replace/>}/></Routes></BrowserRouter></React.StrictMode>);
