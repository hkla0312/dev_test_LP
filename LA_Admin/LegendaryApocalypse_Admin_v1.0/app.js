/* global firebase, FIREBASE_CONFIG */ (() => {   'use strict';    const $ = (selector, root = document) => root.querySelector(selector);   const $$ = (selector, root = document) => [...root.querySelectorAll(selector)];   const escapeHtml = (value = '') => String(value).replace(/[&<>'"]/g, character => ({ '&':'&amp;', '<':'&lt;', '>':'&gt;', "'":'&#39;', '"':'&quot;' }[character]));   const timestampText = value => value && value.toDate ? value.toDate().toLocaleString('ja-JP') : (value || '?');   const serverTime = () => firebase.firestore.FieldValue.serverTimestamp();   const ADMIN_LOGIN_URL = localStorage.getItem('la-admin-login-url') || 'login/index.html';   const SESSION_TIMEOUT_MS = 2 * 60 * 60 * 1000;   let sessionTimer = null;   const state = {     db: null, storage: null, user: null, view: 'events', busy: false,     environment: localStorage.getItem('la-admin-environment') || 'dev',     selectedMember: null, selectedArtist: null, showDeletedSignals: false,     data: { events: [], artists: [], members: [], artistSignals: [], errorReports: [], adminLogs: [] },     settings: { signalEnabled: true, systemEnabled: true }, unsubscribers: []   };   const ACTIONS = ['EVENT_CREATE','EVENT_UPDATE','EVENT_ARCHIVE','EVENT_RESTORE','EVENT_DELETE','ARTIST_CREATE','ARTIST_UPDATE','ARTIST_DELETE','MEMBER_DELETE','PROGRESS_ADD','LICENSE_CHANGE','SIGNAL_DELETE','SIGNAL_RESTORE','ERROR_REPORT_CREATE','ERROR_REPORT_RESOLVE'];   state.filters = { eventSearch:'', eventStatus:'all', artistSearch:'', artistRole:'all', memberSearch:'', memberStatus:'active' };    function toast(message, isError = false) {     const node = document.createElement('div'); node.className = 'toast'; node.textContent = message;     if (isError) node.style.background = '#c83445';     $('#toastRoot').append(node); setTimeout(() => node.remove(), 4200);   }   function errorMessage(error, action) {     const code = error && error.code || '';     if (code.includes('permission-denied')) return `${action}���錠��������܂���BFirestore Rules �ƊǗ��Ҍ�����m�F���Ă��������B`;     if (code.includes('unauthenticated')) return '�F�؏�Ԃ�m�F�ł��܂���B�Ǘ��҃��O�C���y�[�W�����蒼���Ă��������B';     if (code.includes('storage/')) return '�摜�X�g���[�W�ւ̐ڑ��Ɏ��s���܂����BStorage �̗L�����ƃ��[����m�F���Ă��������B';     if (code.includes('unavailable')) return 'Firebase �ɐڑ��ł��܂���B�ʐM��Ԃ�m�F���Ă��������B';     return `${action}�Ɏ��s���܂����B���Ԃ�����čēx���������������B`;   }   function setBanner(message) { const node = $('#firebaseBanner'); node.textContent = message; node.hidden = !message; }   function modal(title, html, submit, submitLabel = '�ۑ�') {     $('#modalRoot').innerHTML = `<div class="modal-backdrop"><section class="modal" role="dialog" aria-modal="true" aria-labelledby="modalTitle"><header class="modal-head"><h2 id="modalTitle">${escapeHtml(title)}</h2><button class="text-button modal-close" aria-label="����">�~</button></header><div class="modal-body">${html}</div><p id="modalSaveState" class="save-state" hidden>���ۑ��̕ύX������܂��B</p><footer class="modal-footer"><button class="secondary modal-cancel">�L�����Z��</button><button class="primary modal-submit">${escapeHtml(submitLabel)}</button></footer></section></div>`;     const close = () => { $('#modalRoot').innerHTML = ''; };     $('.modal-close').onclick = close; $('.modal-cancel').onclick = close;     $('.modal-backdrop').onclick = event => { if (event.target === event.currentTarget) close(); };     const form = $('.modal-body form');     if (form) $$('.modal-body input, .modal-body textarea, .modal-body select').forEach(field => field.addEventListener('input', () => { $('#modalSaveState').hidden = false; }, { once:false }));     $('.modal-submit').onclick = async () => {       if (state.busy) return; state.busy = true; $('.modal-submit').disabled = true; $('.modal-submit').textContent = '�ۑ����c';       try { await submit(); close(); } catch (error) { toast(errorMessage(error, title), true); $('.modal-submit').disabled = false; }       finally { state.busy = false; }     };     $('.modal-submit').focus();   }   document.addEventListener('keydown', event => { if (event.key === 'Escape') $('#modalRoot').innerHTML = ''; });   function confirmAction(title, message, action, label = '���s') { modal(title, `<p>${escapeHtml(message)}</p>`, action, label); }   async function adminLog(actionType, targetType, targetId, targetLabel, detail = '') {     if (!ACTIONS.includes(actionType)) return;     await state.db.collection('adminLogs').add({ actionType, targetType, targetId, targetLabel, detail, adminUid: state.user.uid, adminDisplayName: state.user.displayName || state.user.email || 'ADMIN', createdAt: serverTime() });   }   function collectionSnapshot(name) {     state.unsubscribers.push(state.db.collection(name).onSnapshot(snapshot => {       state.data[name] = snapshot.docs.map(document => ({ id: document.id, ...document.data() }));       if (state.view) render();     }, error => { setBanner(errorMessage(error, '�f�[�^�擾')); }));   }   function level(progress) {     let points = Math.max(0, Number(progress || 0)), current = 1, used = 0, needed = 5;     while (current < 100 && points >= used + needed) { used += needed; current += 1; needed = Math.ceil(5 + (current - 1) * 1.25); }     return { label: `v${(current / 100).toFixed(2)}`, current, start: used, next: current === 100 ? 0 : needed, ratio: current === 100 ? 100 : Math.min(100, Math.round((points - used) / needed * 100)) };   }   function openOperations() {     const signal = state.settings.signalEnabled !== false;     const system = state.settings.systemEnabled !== false;     modal('�^�p�ݒ�', `<div class="form-grid"><label class="field">SIGNAL���M<select id="operationSignal"><option value="true" ${signal ? 'selected' : ''}>ON</option><option value="false" ${!signal ? 'selected' : ''}>OFF</option></select></label><label class="field">�V�X�e���ғ�<select id="operationSystem"><option value="true" ${system ? 'selected' : ''}>ON</option><option value="false" ${!system ? 'selected' : ''}>�ً}��~</option></select></label></div><p class="sub">�ݒ��LA_OS���̑��쐧��ɔ��f����܂��B</p>`, async () => {       const signalEnabled = $('#operationSignal').value === 'true';       const systemEnabled = $('#operationSystem').value === 'true';       await state.db.collection('settings').doc('system').set({ signalEnabled, systemEnabled, updatedAt:serverTime(), updatedBy:state.user.uid }, { merge:true });       toast('�^�p�ݒ��ۑ����܂����B');     }, '�ݒ��ۑ�');   }   function pageHead(title, description, button = '') { return `<div class="page-head"><div><h1>${title}</h1><p class="sub">${description}</p></div>${button}</div>`; }   function table(items, headers, row, empty = '�f�[�^�͂���܂���B') { return `<div class="table-wrap"><table><thead><tr>${headers.map(header => `<th>${header}</th>`).join('')}</tr></thead><tbody>${items.length ? items.map(row).join('') : `<tr><td class="empty" colspan="99">${empty}</td></tr>`}</tbody></table></div>`; }   function eventPage() {     const active = state.data.events.filter(event => event.status !== 'archived').sort((a,b) => String(a.eventDate).localeCompare(String(b.eventDate)));     const archived = state.data.events.filter(event => event.status === 'archived');     const row = event => `<tr><td>${escapeHtml(event.title)}</td><td>${escapeHtml(event.eventDate)}</td><td>${escapeHtml(event.venue)}</td><td>${event.environment === 'prod' && event.lpVisible ? '���J��' : 'DEV / ����J'}</td><td class="actions"><button class="secondary" data-event-edit="${event.id}">�ҏW</button><button class="secondary" data-event-publish="${event.id}">${event.environment === 'prod' ? 'DEV�֖߂�' : '�{�Ԍ��J'}</button>${event.status === 'archived' ? `<button class="secondary" data-event-restore="${event.id}">����</button>` : `<button class="danger" data-event-archive="${event.id}">�A�[�J�C�u</button>`}<button class="danger" data-event-delete="${event.id}">�폜</button></td></tr>`;     return pageHead('EVENT', '�C�x���g�Ǘ�', '<button class="primary" data-action="event-new">�V�K�C�x���g��ǉ�</button>') + `<section class="card"><h2>�J�×\��</h2>${table(active, ['�^�C�g��','�J�Ó�','���','LP','����'], row)}</section><details class="card"><summary>�A�[�J�C�u�ς݁i${archived.length}���j</summary>${table(archived, ['�^�C�g��','�J�Ó�','���','LP','����'], row, '�A�[�J�C�u�ς݂̃C�x���g�͂���܂���B')}</details>`;   }   function artistPage() {     const row = artist => `<tr><td>${escapeHtml(artist.artistKey || '?')}</td><td>${escapeHtml(artist.name)}</td><td>${escapeHtml(artist.role || 'FRESH')}</td><td>${Number(artist.appearanceCount || 0)}</td><td>${escapeHtml(artist.genre || '?')}</td><td>${artist.environment === 'prod' && artist.lpVisible ? '���J��' : 'DEV / ����J'}</td><td class="actions"><button class="secondary" data-artist-edit="${artist.id}">�ҏW</button><button class="secondary" data-artist-publish="${artist.id}">${artist.environment === 'prod' ? 'DEV�֖߂�' : '�{�Ԍ��J'}</button><button class="danger" data-artist-delete="${artist.id}">�폜</button></td></tr>`;     return pageHead('ARTIST', '�A�[�e�B�X�g�Ǘ�', '<button class="primary" data-action="artist-new">�V�K�A�[�e�B�X�g��ǉ�</button>') + `<section class="card">${table(state.data.artists, ['ARTIST KEY','���O','���[��','�o����','�W������','LP','����'], row)}</section>`;   }   function memberPage() {     const row = member => { const version = level(member.progress); return `<tr><td>${escapeHtml(member.memberId)}</td><td>${escapeHtml(member.displayName)}</td><td>${version.label}</td><td>${escapeHtml(member.email)}</td><td>${member.emailVerified ? '�F�؍ς�' : '���F��'}</td><td>${Number(member.progress || 0)} pt</td><td>${escapeHtml(member.licenseType || 'NONE')}</td><td>${escapeHtml(member.accountStatus || 'active')}</td><td><button class="secondary" data-member-select="${member.id}">�I��</button><button class="danger" data-member-delete="${member.id}">�폜</button></td></tr>`; };     const current = state.selectedMember ? `<section class="card"><h2>�I�𒆁F${escapeHtml(state.selectedMember.displayName)}</h2><p>${escapeHtml(state.selectedMember.memberId)} / ${level(state.selectedMember.progress).label} / ${Number(state.selectedMember.progress || 0)} pt</p><label class="field">���C�Z���X<select id="licenseSelect">${['NONE','STANDARD','PREMIUM'].map(value => `<option ${state.selectedMember.licenseType === value ? 'selected' : ''}>${value}</option>`).join('')}</select></label><button class="primary" data-action="license-save">���C�Z���X��ύX</button></section>` : '';     return pageHead('MEMBER', '�����o�[����Firebase Authentication�ƘA�g���Ă��܂��B') + `<section class="card">${table(state.data.members, ['MEMBER ID','�\����','VERSION','���[��','�F��','PROGRESS','���C�Z���X','���','����'], row)}</section>${current}`;   }   function progressPage() {     const selected = state.selectedMember;     const choices = state.data.members.filter(member => member.accountStatus !== 'deleted').sort((a,b) => String(a.displayName).localeCompare(String(b.displayName)));     const reasonOptions = [['���O�C��',1],['DANMAKU���M',2],['SIGNAL���M',3],['����',10],['���́F����',5],['���́F�h�����N',10],['���́F�A�[�J�C�u���C�Z���X',25],['���́F�A�p����',60],['�C�x���g���T',50],['�^�c����','']];     return pageHead('PROGRESS', '�����o�[��I����A�t�^��e��m�F���Ă�����s���܂��B') + `<section class="split"><section class="card"><h2>1. �����o�[�I��</h2><label class="field">MEMBER ID�𒼐ړ���<input id="progressMemberId" value="${escapeHtml(selected ? selected.memberId : '')}" placeholder="��FA7K2"></label><button class="secondary" data-action="progress-id-select">ID�őI��</button><label class="field">�o�^�ς݃����o�[����I��<select id="progressMemberList"><option value="">�I����Ă�������</option>${choices.map(member => `<option value="${member.id}" ${selected && member.id === selected.id ? 'selected' : ''}>${escapeHtml(member.memberId)} / ${escapeHtml(member.displayName)}</option>`).join('')}</select></label>${selected ? `<p><strong>${escapeHtml(selected.displayName)}</strong><br>${escapeHtml(selected.memberId)} / ${level(selected.progress).label} / ${Number(selected.progress || 0)} pt</p>` : '<p class="sub">�Ώۃ����o�[��I����Ă��������B</p>'}</section><form class="card" id="progressForm"><h2>2. PROGRESS�t�^</h2><div class="preset-grid">${[1,2,3,5,10,25,50,60,100,300,500].map(value => `<button type="button" class="secondary" data-progress-preset="${value}">+${value}</button>`).join('')}</div><label class="field">�t�^�|�C���g<input name="amount" type="number" min="1" required></label><label class="field">���R<select name="reason" required><option value="">�I����Ă�������</option>${reasonOptions.map(([label, value]) => `<option value="${escapeHtml(label)}" data-points="${value}">${escapeHtml(label)}${value !== '' ? `�i+${value}�j` : ''}</option>`).join('')}</select></label><button class="primary" ${selected ? '' : 'disabled'}>�m�F���ĕt�^</button></form></section>`;   }   function signalPage() {     const filter = state.filters;     const signals = [...state.data.artistSignals]       .filter(signal => state.showDeletedSignals || !signal.isDeleted)       .filter(signal => filter.signalArtist === 'all' || signal.artistId === filter.signalArtist)       .filter(signal => filter.signalType === 'all' || signal.signalType === filter.signalType)       .filter(signal => filterText(signal, ['memberId', 'memberDisplayName', 'comment', 'artistName'], filter.signalSearch))       .sort((a, b) => String(b.createdAt).localeCompare(String(a.createdAt)));     const stats = {       total: state.data.artistSignals.length,       visible: signals.length,       deleted: state.data.artistSignals.filter(signal => signal.isDeleted).length,     };     const filterBar = `<div class="list-filter"><input id="signalSearch" value="${escapeHtml(filter.signalSearch)}" placeholder="MEMBER ID�E�\�����E�R�����g�Ō���"><select id="signalArtistFilter"><option value="all">�S�A�[�e�B�X�g</option>${state.data.artists.map(item => `<option value="${item.id}" ${filter.signalArtist === item.id ? 'selected' : ''}>${escapeHtml(item.artistKey)} / ${escapeHtml(item.name)}</option>`).join('')}</select><select id="signalTypeFilter"><option value="all">�S���</option>${['song','stage','character','other'].map(type => `<option value="${type}" ${filter.signalType === type ? 'selected' : ''}>${escapeHtml(type)}</option>`).join('')}</select><span class="filter-count">${signals.length}��</span></div>`;     const summary = `<div class="form-grid"><div class="summary">���� ${stats.total}</div><div class="summary">�\���� ${stats.visible}</div><div class="summary">�폜�ς� ${stats.deleted}</div><div class="summary">${state.showDeletedSignals ? '�폜�ςݕ\����' : '�폜�ςݔ�\��'}</div></div>`;     const body = table(signals, ['ARTIST','MEMBER ID','�\����','���','�R�����g','���M����','���','����'], signal => `<tr><td>${escapeHtml(signal.artistName || signal.artistId)}</td><td>${escapeHtml(signal.memberId)}</td><td>${escapeHtml(signal.memberDisplayName)}</td><td>${escapeHtml(signal.signalType)}</td><td>${escapeHtml(signal.comment)}</td><td>${timestampText(signal.createdAt)}</td><td>${signal.isDeleted ? '�폜�ς�' : '�\����'}</td><td>${signal.isDeleted ? `<button class="secondary" data-signal-restore="${signal.id}">����</button>` : `<button class="danger" data-signal-delete="${signal.id}">�폜</button>`}</td></tr>`, 'SIGNAL�͂���܂���B');     return pageHead('SIGNAL', '��M����SIGNAL��{���E�폜�E�����ł��܂��B') + `<section class="card">${summary}</section><section class="card">${filterBar}<label><input type="checkbox" id="signalDeletedToggle" ${state.showDeletedSignals ? 'checked' : ''}> �폜�ς݂�\��</label></section><section class="card">${body}</section>`;   }   function reportPage() {     const reports = [...state.data.errorReports].sort((a, b) => String(b.createdAt).localeCompare(String(a.createdAt)));     const unresolved = reports.filter(report => report.status !== "resolved").length;     const reportTone = report => {       const category = String(report.errorCategory || "").toLowerCase();       if (report.status === "resolved") return "report-row--resolved";       if (category === "policy") return "report-row--policy";       if (category === "validation") return "report-row--validation";       if (category === "network") return "report-row--network";       if (category === "auth") return "report-row--auth";       return "report-row--backend";     };     const row = report => `<tr class="report-row ${reportTone(report)}"><td>${timestampText(report.createdAt)}</td><td>${escapeHtml(report.source || "laos")}</td><td>${escapeHtml(report.area || "system")}</td><td>${escapeHtml(report.action || "report")}</td><td><span class="report-code">${escapeHtml(report.errorCode || "unknown")}</span></td><td>${escapeHtml(report.message || "no message")}</td><td>${escapeHtml(report.displayName || report.memberId || "unknown")}</td><td><span class="report-status">${escapeHtml(report.status || "new")}</span></td><td>${report.status !== "resolved" ? `<button class="secondary" data-report-resolve="${report.id}">����ς�</button>` : `<span class="tag off">RESOLVED</span>`}</td></tr>`;     const catalog = (window.LAErrorReporting?.codeCatalog || []).slice();     const troubleshootingRows = catalog.map(item => `<tr><td>${escapeHtml(item.code)}</td><td>${escapeHtml(item.category)}</td><td>${escapeHtml(item.title)}</td><td>${escapeHtml(item.situation)}</td><td>${escapeHtml(item.userAction)}</td></tr>`);     return pageHead("REPORT", "���M���O�Ƒz��G���[��m�F�ł��܂��B", `<div class="report-summary"><span class="tag">TOTAL ${reports.length}</span><span class="tag off">OPEN ${unresolved}</span></div>`) +       '<section class="card">' + table(reports, ["DATE","SOURCE","AREA","ACTION","CODE","MESSAGE","USER","STATUS","����"], row, "No reports yet.") + '</section>' +       '<section class="card"><h2>�g���u���V���[�e�B���O</h2><p class="sub">OS �̑��M���s���ɎQ�Ƃ���z��G���[�R�[�h�ꗗ�ł��B</p>' +       table(troubleshootingRows, ["CODE","CATEGORY","TITLE","SITUATION","USER ACTION"], item => item, "�Q�Ƃł���G���[�R�[�h������܂���B") +       '</section>';   }   function logPage() {     const actions = [...new Set(state.data.adminLogs.map(log => log.actionType))];     return pageHead('CHANGE LOG', '��v�ȑ��엚��i�ǂݎ���p�j') + `<section class="card"><div class="form-grid"><label class="field">������<select id="logAction"><option value="">���ׂ�</option>${actions.map(action => `<option>${escapeHtml(action)}</option>`).join('')}</select></label><label class="field">�Ώی���<input id="logSearch" placeholder="�Ώۖ��Ō���"></label><label class="field">�J�n��<input id="logFrom" type="date"></label><label class="field">�I����<input id="logTo" type="date"></label></div></section><section class="card"><div id="logTable">${renderLogs()}</div></section>`;   }   function renderLogs() { return table([...state.data.adminLogs].sort((a,b) => String(b.createdAt).localeCompare(String(a.createdAt))), ['����','�Ǘ���','����','�Ώ�','�ڍ�'], log => `<tr><td>${timestampText(log.createdAt)}</td><td>${escapeHtml(log.adminDisplayName)}</td><td>${escapeHtml(log.actionType)}</td><td>${escapeHtml(log.targetLabel)}</td><td>${escapeHtml(log.detail)}</td></tr>`); }   function actionMenu(content) { return `<details class="action-menu"><summary>����</summary><div class="action-menu-body">${content}</div></details>`; }   function filterText(item, keys, query) { return !query || keys.some(key => String(item[key] || '').toLowerCase().includes(query.toLowerCase())); }   function updateErrorBadge() {     const badge = $('#errorReportBadge');     if (!badge) return;     const unread = state.data.errorReports.filter(report => report.status !== 'resolved').length;     badge.textContent = `ERROR: ${unread}`;     badge.classList.toggle('off', unread === 0);   }   function eventPage() {     const filter = state.filters;     const matches = event => filterText(event, ['title','venue','eventDate'], filter.eventSearch) && (filter.eventStatus === 'all' || (filter.eventStatus === 'published' ? event.environment === 'prod' && event.lpVisible : event.environment !== 'prod' || !event.lpVisible));     const active = state.data.events.filter(event => event.status !== 'archived' && matches(event)).sort((a,b) => String(a.eventDate).localeCompare(String(b.eventDate)));     const archived = state.data.events.filter(event => event.status === 'archived' && matches(event));     const row = event => `<tr><td>${escapeHtml(event.title)}</td><td>${escapeHtml(event.eventDate)}</td><td>${escapeHtml(event.venue)}</td><td>${event.environment === 'prod' && event.lpVisible ? '���J��' : 'DEV / ����J'}</td><td><button class="primary compact" data-event-edit="${event.id}">�ҏW</button>${actionMenu(`<button class="secondary" data-event-publish="${event.id}">${event.environment === 'prod' ? 'DEV�֖߂�' : '�{�Ԍ��J'}</button>${event.status === 'archived' ? `<button class="secondary" data-event-restore="${event.id}">����</button>` : `<button class="secondary" data-event-archive="${event.id}">�A�[�J�C�u</button>`}<button class="danger" data-event-delete="${event.id}">�폜</button>`)}</td></tr>`;     const filterBar = `<div class="list-filter"><input id="eventSearch" value="${escapeHtml(filter.eventSearch)}" placeholder="�^�C�g���E���E�J�Ó��Ō���"><select id="eventStatus"><option value="all">���J��ԁF���ׂ�</option><option value="published" ${filter.eventStatus === 'published' ? 'selected' : ''}>LP���J��</option><option value="private" ${filter.eventStatus === 'private' ? 'selected' : ''}>DEV / ����J</option></select><span class="filter-count">${active.length}��</span></div>`;     return pageHead('EVENT', '�C�x���g�Ǘ�', '<button class="primary" data-action="event-new">�V�K�C�x���g��ǉ�</button>') + `<section class="card"><h2>�J�×\��</h2>${filterBar}${table(active, ['�^�C�g��','�J�Ó�','���','LP','����'], row)}</section><details class="card"><summary>�A�[�J�C�u�ς݁i${archived.length}���j</summary>${table(archived, ['�^�C�g��','�J�Ó�','���','LP','����'], row, '�A�[�J�C�u�ς݂̃C�x���g�͂���܂���B')}</details>`;   }   function artistPage() {     const filter = state.filters;     const artists = state.data.artists.filter(artist => filterText(artist, ['artistKey','name','genre'], filter.artistSearch) && (filter.artistRole === 'all' || artist.role === filter.artistRole));     const row = artist => `<tr><td>${escapeHtml(artist.artistKey || '?')}</td><td>${escapeHtml(artist.name)}</td><td>${escapeHtml(artist.role || 'FRESH')}</td><td>${Number(artist.appearanceCount || 0)}</td><td>${escapeHtml(artist.genre || '?')}</td><td>${artist.environment === 'prod' && artist.lpVisible ? '���J��' : 'DEV / ����J'}</td><td><button class="primary compact" data-artist-edit="${artist.id}">�ҏW</button>${actionMenu(`<button class="secondary" data-artist-publish="${artist.id}">${artist.environment === 'prod' ? 'DEV�֖߂�' : '�{�Ԍ��J'}</button><button class="danger" data-artist-delete="${artist.id}">�폜</button>`)}</td></tr>`;     return pageHead('ARTIST', '�A�[�e�B�X�g�Ǘ�', '<button class="primary" data-action="artist-new">�V�K�A�[�e�B�X�g��ǉ�</button>') + `<section class="card"><div class="list-filter"><input id="artistSearch" value="${escapeHtml(filter.artistSearch)}" placeholder="���O�E�L�[�E�W�������Ō���"><select id="artistRole"><option value="all">���[���F���ׂ�</option>${['REGULAR','CORE','FRESH','ORGANIZER'].map(role => `<option ${filter.artistRole === role ? 'selected' : ''}>${role}</option>`).join('')}</select><span class="filter-count">${artists.length}��</span></div>${table(artists, ['ARTIST KEY','���O','���[��','�o����','�W������','LP','����'], row)}</section>`;   }   function memberPage() {     const filter = state.filters;     const members = state.data.members.filter(member => filterText(member, ['memberId','displayName','email'], filter.memberSearch) && (filter.memberStatus === 'all' || member.accountStatus !== 'deleted'));     const row = member => { const version = level(member.progress); return `<tr><td>${escapeHtml(member.memberId)}</td><td>${escapeHtml(member.displayName)}</td><td>${version.label}</td><td>${escapeHtml(member.email)}</td><td>${member.emailVerified ? '�F�؍ς�' : '���F��'}</td><td>${Number(member.progress || 0)} pt</td><td>${escapeHtml(member.licenseType || 'NONE')}</td><td>${escapeHtml(member.accountStatus || 'active')}</td><td><button class="primary compact" data-member-select="${member.id}">�ڍ�</button>${member.accountStatus !== 'deleted' ? actionMenu(`<button class="danger" data-member-delete="${member.id}">�폜</button>`) : ''}</td></tr>`; };     const current = state.selectedMember ? `<section class="card"><h2>�I�𒆁F${escapeHtml(state.selectedMember.displayName)}</h2><p>${escapeHtml(state.selectedMember.memberId)} / ${level(state.selectedMember.progress).label} / ${Number(state.selectedMember.progress || 0)} pt</p><label class="field">���C�Z���X<select id="licenseSelect">${['NONE','STANDARD','PREMIUM'].map(value => `<option ${state.selectedMember.licenseType === value ? 'selected' : ''}>${value}</option>`).join('')}</select></label><button class="primary" data-action="license-save">���C�Z���X��ύX</button></section>` : '';     return pageHead('MEMBER', '�����o�[����Firebase Authentication�ƘA�g���Ă��܂��B') + `<section class="card"><div class="list-filter"><input id="memberSearch" value="${escapeHtml(filter.memberSearch)}" placeholder="ID�E�\�����E���[���Ō���"><select id="memberStatus"><option value="active">�L�������o�[</option><option value="all" ${filter.memberStatus === 'all' ? 'selected' : ''}>�폜�����܂�</option></select><span class="filter-count">${members.length}��</span></div>${table(members, ['MEMBER ID','�\����','VERSION','���[��','�F��','PROGRESS','���C�Z���X','���','����'], row)}</section>${current}`;   }   function render() {     if (!state.user || !state.view) return;     const pages = { events: eventPage, artists: artistPage, members: memberPage, progress: progressPage, signals: signalPage, reports: reportPage, logs: logPage };     $$('.nav-item[data-view]').forEach(button => button.classList.toggle('active', button.dataset.view === state.view));     $('#content').innerHTML = pages[state.view](); bindPage();     updateErrorBadge();   }   function formValue(form) { const values = Object.fromEntries(new FormData(form)); ['advancePrice','doorPrice','streamingPrice','specialAppearanceCount'].forEach(key => { if (key in values) values[key] = Number(values[key] || 0); }); values.lpVisible = values.lpVisible === 'true'; return values; }   function eventForm(event = {}) { return `<form id="eventForm"><div class="form-grid"><label class="field">�^�C�g��<input name="title" required value="${escapeHtml(event.title)}"></label><label class="field">�J�Ó�<input name="eventDate" type="date" required value="${escapeHtml(event.eventDate)}"></label><label class="field">���<input name="venue" required value="${escapeHtml(event.venue)}"></label><label class="field">OPEN<input name="openTime" type="time" value="${escapeHtml(event.openTime)}"></label><label class="field">START<input name="startTime" type="time" value="${escapeHtml(event.startTime)}"></label><label class="field">�O������<input name="advancePrice" type="number" min="0" value="${Number(event.advancePrice || 0)}"></label><label class="field">��������<input name="doorPrice" type="number" min="0" value="${Number(event.doorPrice || 0)}"></label><label class="field">�z�M����<input name="streamingPrice" type="number" min="0" value="${Number(event.streamingPrice || 0)}"></label><label class="field">�c�C�L���XURL<input name="streamingUrl" type="url" value="${escapeHtml(event.streamingUrl)}"></label><label class="field">LP���J<select name="lpVisible"><option value="true" ${event.lpVisible !== false ? 'selected' : ''}>ON</option><option value="false" ${event.lpVisible === false ? 'selected' : ''}>OFF</option></select></label><div class="wide"><h3>�o���A�[�e�B�X�g</h3><button class="secondary" type="button" data-action="event-artist-picker">�A�[�e�B�X�g��ǉ�</button><div id="eventArtists" class="artist-list">${eventArtistTags(event.artistIds || [])}</div></div></div></form>`; }   function eventArtistTags(ids) { return ids.map(id => state.data.artists.find(artist => artist.id === id)).filter(Boolean).map(artist => `<span class="artist-tag" draggable="true" data-artist-id="${artist.id}">${escapeHtml(artist.name)} <button type="button" class="text-button" data-event-artist-remove="${artist.id}" aria-label="${escapeHtml(artist.name)}��O��">�~</button></span>`).join('') || '<p class="sub">�o���A�[�e�B�X�g�͖��ݒ�ł��B</p>'; }   function artistForm(artist = {}) { const roles = ['REGULAR','CORE','FRESH','ORGANIZER']; return `<form id="artistForm"><div class="form-grid"><label class="field">���O<input name="name" required value="${escapeHtml(artist.name)}"></label><label class="field">���[��<select name="role">${roles.map(role => `<option ${artist.role === role ? 'selected' : ''}>${role}</option>`).join('')}</select></label><label class="field">�W������<input name="genre" value="${escapeHtml(artist.genre)}" placeholder="���R����"></label><label class="field">�ߋ��o�����ʃJ�E���g<input name="specialAppearanceCount" type="number" min="0" value="${Number(artist.specialAppearanceCount || 0)}"></label><label class="field wide">�v���t�B�[��<textarea name="profile">${escapeHtml(artist.profile)}</textarea></label><label class="field">X<input name="xUrl" type="url" value="${escapeHtml(artist.xUrl)}"></label><label class="field">YouTube<input name="youtubeUrl" type="url" value="${escapeHtml(artist.youtubeUrl)}"></label><label class="field">SNS�����N1<input name="snsUrl1" type="url" value="${escapeHtml(artist.snsUrl1)}"></label><label class="field">SNS�����N2<input name="snsUrl2" type="url" value="${escapeHtml(artist.snsUrl2)}"></label><label class="field">LP�f��<select name="lpVisible"><option value="true" ${artist.lpVisible !== false ? 'selected' : ''}>ON</option><option value="false" ${artist.lpVisible === false ? 'selected' : ''}>OFF</option></select></label><label class="field">�A�C�R���摜<input name="image" type="file" accept="image/png,image/jpeg,image/webp"></label></div><p class="sub">�C�x���g�o���񐔂Ɖߋ��o�����ʃJ�E���g����Z���ĕ\�����܂��B</p></form>`; }   async function nextArtistKey() { return state.db.runTransaction(async transaction => { const ref = state.db.collection('counters').doc('artist'); const snap = await transaction.get(ref); const number = Number(snap.exists ? snap.data().value || 0 : 0) + 1; transaction.set(ref, { value: number }, { merge: true }); return `ART-${String(number).padStart(4,'0')}`; }); }   async function uploadImage(file, folder) {     if (!file) return null;     if (!['image/png','image/jpeg','image/webp'].includes(file.type) || file.size > 20 * 1024 * 1024) throw new Error('storage/invalid-file');     const ref = state.storage.ref().child(`${folder}/${Date.now()}-${file.name.replace(/[^a-zA-Z0-9._-]/g, '_')}`);     const task = ref.put(file, { contentType: file.type });     return new Promise((resolve, reject) => task.on('state_changed', snapshot => setBanner(`�摜��A�b�v���[�h���F${Math.round(snapshot.bytesTransferred / snapshot.totalBytes * 100)}%`), reject, async () => { setBanner(''); resolve(await task.snapshot.ref.getDownloadURL()); }));   }   function openEvent(event) {     const isNew = !event; const draft = event ? { ...event, artistIds: [...(event.artistIds || [])] } : { artistIds: [], lpVisible: true };     modal(isNew ? '�V�K�C�x���g' : '�C�x���g��ҏW', eventForm(draft), async () => {       const form = $('#eventForm'), values = formValue(form); values.artistIds = draft.artistIds; values.environment = event ? event.environment || state.environment : state.environment; values.updatedAt = serverTime();       const file = form.elements.flyer && form.elements.flyer.files[0]; if (file) values.flyerUrl = await uploadImage(file, 'events');       if (event) { await state.db.collection('events').doc(event.id).update(values); await adminLog('EVENT_UPDATE','event',event.id,values.title,'�C�x���g��X�V'); }       else { values.status = 'active'; values.createdAt = serverTime(); const ref = await state.db.collection('events').add(values); await adminLog('EVENT_CREATE','event',ref.id,values.title,'�C�x���g��쐬'); }       toast('�C�x���g��ۑ����܂����B');     });     const imageField = document.createElement('label');     imageField.className = 'field';     imageField.innerHTML = '�t���C���[�摜<input name="flyer" type="file" accept="image/png,image/jpeg,image/webp">';     $('#eventForm .form-grid').append(imageField);     const refreshTags = () => { $('#eventArtists').innerHTML = eventArtistTags(draft.artistIds); bindEventArtistTags(); };     const bindEventArtistTags = () => $$('[data-event-artist-remove]').forEach(button => button.onclick = () => { draft.artistIds = draft.artistIds.filter(id => id !== button.dataset.eventArtistRemove); refreshTags(); });     bindEventArtistTags();     $('[data-action="event-artist-picker"]').onclick = () => openArtistPicker(draft, refreshTags);   }   function openArtistPicker(draft, refresh) {     const tags = state.data.artists.map(artist => `<button type="button" class="artist-select-tag ${draft.artistIds.includes(artist.id) ? 'selected' : ''}" data-picker-id="${artist.id}">${escapeHtml(artist.name)} <small>${escapeHtml(artist.role || 'FRESH')}</small></button>`).join('');     modal('�A�[�e�B�X�g��ǉ�', `<p>�^�O��N���b�N���đI����܂��B�d���ǉ��͂���܂���B</p><div class="artist-picker">${tags || '<p class="empty">�o�^�ς݃A�[�e�B�X�g�͂��܂���B</p>'}</div><hr><label class="field">���o�^�A�[�e�B�X�g��<input id="quickArtistName" placeholder="���O�����ŐV�K�o�^"></label><button type="button" class="secondary" id="quickArtistAdd">���O�����ŐV�K�ǉ�</button>`, async () => { refresh(); toast('�C�x���g�̏o���A�[�e�B�X�g��X�V���܂����B'); }, '�ǉ���m��');     $$('[data-picker-id]').forEach(button => button.onclick = () => { const id = button.dataset.pickerId; draft.artistIds = draft.artistIds.includes(id) ? draft.artistIds.filter(value => value !== id) : [...draft.artistIds, id]; button.classList.toggle('selected', draft.artistIds.includes(id)); });     $('#quickArtistAdd').onclick = async () => {       const name = $('#quickArtistName').value.trim(); if (!name) { toast('���O����͂��Ă��������B', true); return; }       try { const key = await nextArtistKey(); const ref = await state.db.collection('artists').add({ artistKey:key, name, profileCompleted:false, role:'FRESH', lpVisible:false, environment:state.environment, specialAppearanceCount:0, appearanceCount:0, createdAt:serverTime(), updatedAt:serverTime() }); draft.artistIds = [...new Set([...draft.artistIds, ref.id])]; await adminLog('ARTIST_CREATE','artist',ref.id,name,'�C�x���g�ҏW����ȈՓo�^'); toast('�A�[�e�B�X�g��ǉ����܂����B'); $('#modalRoot').innerHTML = ''; openArtistPicker(draft, refresh); } catch (error) { toast(errorMessage(error, '�A�[�e�B�X�g�ǉ�'), true); }     };   }   function openArtist(artist) {     const isNew = !artist;     modal(isNew ? '�V�K�A�[�e�B�X�g' : '�A�[�e�B�X�g��ҏW', artistForm(artist || { lpVisible:true, role:'FRESH' }), async () => {       const form = $('#artistForm'), values = formValue(form); values.updatedAt = serverTime(); values.environment = artist ? artist.environment || state.environment : state.environment;       const special = Number(values.specialAppearanceCount || 0), eventCount = state.data.events.filter(event => (event.artistIds || []).includes(artist && artist.id)).length; values.eventAppearanceCount = eventCount; values.appearanceCount = eventCount + special;       const file = form.elements.image.files[0]; if (file) values.imageUrl = await uploadImage(file, 'artists');       if (artist) { await state.db.collection('artists').doc(artist.id).update(values); await adminLog('ARTIST_UPDATE','artist',artist.id,values.name,'�A�[�e�B�X�g��X�V'); }       else { values.artistKey = await nextArtistKey(); values.createdAt = serverTime(); const ref = await state.db.collection('artists').add(values); await adminLog('ARTIST_CREATE','artist',ref.id,values.name,'�A�[�e�B�X�g��쐬'); }       toast('�A�[�e�B�X�g��ۑ����܂����B');     });   }   async function setEnvironment(collection, item, environment) { const warning = environment === 'prod' ? '�{�Ԍ��J���܂��BLP�֕\�������ݒ�ł��B' : '�J�����֖߂��܂��B'; confirmAction(environment === 'prod' ? '�{�Ԍ��J' : 'DEV�֖߂�', warning, async () => { await state.db.collection(collection).doc(item.id).update({ environment, updatedAt:serverTime() }); await adminLog(collection === 'events' ? 'EVENT_UPDATE' : 'ARTIST_UPDATE', collection.slice(0,-1), item.id, item.title || item.name, `environment �� ${environment} �ɕύX`); localStorage.setItem('la-admin-environment', environment); toast('���ݒ��X�V���܂����B'); }, environment === 'prod' ? '�{�Ԍ��J����' : 'DEV�֖߂�'); }   function bindPage() {     $('[data-action="event-new"]') && ($('[data-action="event-new"]').onclick = () => openEvent());     $('[data-action="artist-new"]') && ($('[data-action="artist-new"]').onclick = () => openArtist());     $$('[data-event-edit]').forEach(button => button.onclick = () => openEvent(state.data.events.find(item => item.id === button.dataset.eventEdit)));     $$('[data-artist-edit]').forEach(button => button.onclick = () => openArtist(state.data.artists.find(item => item.id === button.dataset.artistEdit)));     $$('[data-event-publish]').forEach(button => button.onclick = () => setEnvironment('events', state.data.events.find(item => item.id === button.dataset.eventPublish), state.data.events.find(item => item.id === button.dataset.eventPublish).environment === 'prod' ? 'dev' : 'prod'));     $$('[data-artist-publish]').forEach(button => button.onclick = () => setEnvironment('artists', state.data.artists.find(item => item.id === button.dataset.artistPublish), state.data.artists.find(item => item.id === button.dataset.artistPublish).environment === 'prod' ? 'dev' : 'prod'));     $$('[data-event-archive]').forEach(button => button.onclick = () => confirmAction('�C�x���g��A�[�J�C�u', 'LP�����\���ɂȂ�܂��B', async () => { const item = state.data.events.find(event => event.id === button.dataset.eventArchive); await state.db.collection('events').doc(item.id).update({ status:'archived', updatedAt:serverTime() }); await adminLog('EVENT_ARCHIVE','event',item.id,item.title,'�A�[�J�C�u'); toast('�A�[�J�C�u���܂����B'); }, '�A�[�J�C�u'));     $$('[data-event-restore]').forEach(button => button.onclick = () => confirmAction('�C�x���g�𕜌�', '�J�×\��֖߂��܂��B', async () => { const item = state.data.events.find(event => event.id === button.dataset.eventRestore); await state.db.collection('events').doc(item.id).update({ status:'active', updatedAt:serverTime() }); await adminLog('EVENT_RESTORE','event',item.id,item.title,'����'); toast('�������܂����B'); }, '����'));     $$('[data-event-delete]').forEach(button => button.onclick = () => confirmAction('�C�x���g��폜', '�����폜���܂��B���̑���͌��ɖ߂��܂���B', async () => { const item = state.data.events.find(event => event.id === button.dataset.eventDelete); await state.db.collection('events').doc(item.id).delete(); await adminLog('EVENT_DELETE','event',item.id,item.title,'�����폜'); toast('�폜���܂����B'); }, '�폜����'));     $$('[data-artist-delete]').forEach(button => button.onclick = () => confirmAction('�A�[�e�B�X�g��폜', '�����폜���܂��B���̑���͌��ɖ߂��܂���B', async () => { const item = state.data.artists.find(artist => artist.id === button.dataset.artistDelete); await state.db.collection('artists').doc(item.id).delete(); await adminLog('ARTIST_DELETE','artist',item.id,item.name,'�����폜'); toast('�폜���܂����B'); }, '�폜����'));     $$('[data-member-select]').forEach(button => button.onclick = () => { state.selectedMember = state.data.members.find(member => member.id === button.dataset.memberSelect); render(); });     $$('[data-member-delete]').forEach(button => button.onclick = () => confirmAction('�����o�[��폜', '�폜����Ƃ��Ĉꗗ�Ɏc���܂��B', async () => { const member = state.data.members.find(item => item.id === button.dataset.memberDelete); await state.db.collection('members').doc(member.id).update({ accountStatus:'deleted', deletedAt:serverTime(), deletedBy:state.user.uid, updatedAt:serverTime() }); await adminLog('MEMBER_DELETE','member',member.id,member.displayName,'�_���폜'); toast('�����o�[��폜���܂����B'); }, '�폜����'));     $('[data-action="license-save"]') && ($('[data-action="license-save"]').onclick = () => { const value = $('#licenseSelect').value, member = state.selectedMember; confirmAction('���C�Z���X��ύX', `${member.displayName} �̃��C�Z���X�� ${value} �ɕύX���܂��B`, async () => { await state.db.collection('members').doc(member.id).update({ licenseType:value, updatedAt:serverTime() }); await adminLog('LICENSE_CHANGE','member',member.id,member.displayName,`licenseType: ${value}`); toast('���C�Z���X��X�V���܂����B'); }, '�ύX����'); });     $('[data-action="progress-id-select"]') && ($('[data-action="progress-id-select"]').onclick = () => { const id = $('#progressMemberId').value.trim().toUpperCase(); const member = state.data.members.find(item => String(item.memberId).toUpperCase() === id); if (!member) { toast('��v����MEMBER ID������܂���B', true); return; } state.selectedMember = member; render(); });     $('#progressMemberList') && ($('#progressMemberList').onchange = event => { state.selectedMember = state.data.members.find(member => member.id === event.target.value) || null; render(); });     $$('[data-progress-preset]').forEach(button => button.onclick = () => { $('#progressForm').elements.amount.value = button.dataset.progressPreset; });     $('#progressForm') && ($('#progressForm').onsubmit = event => { event.preventDefault(); const form = event.currentTarget, amount = Number(form.elements.amount.value), reason = form.elements.reason.value; if (!Number.isInteger(amount) || amount < 1 || !reason) { toast('�|�C���g�Ɨ��R����͂��Ă��������B', true); return; } const member = state.selectedMember, before = Number(member.progress || 0), after = before + amount; confirmAction('PROGRESS��t�^', `${member.displayName}�i${member.memberId}�j�֕t�^���܂��B���݁F${before} pt / ${level(before).label} �� �t�^��F${after} pt / ${level(after).label}�B���R�F${reason}`, async () => { const memberRef = state.db.collection('members').doc(member.id), logRef = state.db.collection('progressLogs').doc(), adminRef = state.db.collection('adminLogs').doc(); await state.db.runTransaction(async transaction => { const snap = await transaction.get(memberRef); transaction.update(memberRef, { progress:Number(snap.data().progress || 0) + amount, updatedAt:serverTime() }); transaction.set(logRef, { memberUid:member.id, memberId:member.memberId, amount, reason, grantedBy:state.user.uid, createdAt:serverTime() }); transaction.set(adminRef, { actionType:'PROGRESS_ADD', targetType:'member', targetId:member.id, targetLabel:member.displayName, detail:`+${amount} / ${reason}`, adminUid:state.user.uid, adminDisplayName:state.user.displayName || state.user.email, createdAt:serverTime() }); }); toast('PROGRESS��t�^���܂����B'); }, '�t�^����'); });     $('#signalArtist') && ($('#signalArtist').onchange = event => { state.selectedArtist = state.data.artists.find(item => item.id === event.target.value) || null; render(); });     $('#signalDeletedToggle') && ($('#signalDeletedToggle').onchange = event => { state.showDeletedSignals = event.target.checked; render(); });     $$('[data-signal-delete]').forEach(button => button.onclick = () => confirmAction('SIGNAL��폜', '�폜�ς݂Ƃ��Ĕ�\���ɂ��܂��B', async () => { const item = state.data.artistSignals.find(signal => signal.id === button.dataset.signalDelete); await state.db.collection('artistSignals').doc(item.id).update({ isDeleted:true, deletedAt:serverTime(), deletedBy:state.user.uid }); await adminLog('SIGNAL_DELETE','signal',item.id,item.memberDisplayName || item.memberId,'�_���폜'); toast('SIGNAL��폜���܂����B'); }, '�폜����'));     $$('[data-signal-restore]').forEach(button => button.onclick = () => confirmAction('SIGNAL�𕜌�', '�폜��Ԃ������܂��B', async () => { const item = state.data.artistSignals.find(signal => signal.id === button.dataset.signalRestore); await state.db.collection('artistSignals').doc(item.id).update({ isDeleted:false, deletedAt:null, deletedBy:null }); await adminLog('SIGNAL_RESTORE','signal',item.id,item.memberDisplayName || item.memberId,'����'); toast('SIGNAL�𕜌����܂����B'); }, '��������'));     $$("[data-report-resolve]").forEach(button => button.onclick = () => confirmAction("�񍐂���", "���̕񍐂����ς݂ɂ��܂��B", async () => { const item = state.data.errorReports.find(report => report.id === button.dataset.reportResolve); await state.db.collection("errorReports").doc(item.id).update({ status:"resolved", resolvedAt:serverTime(), resolvedBy:state.user.uid, updatedAt:serverTime() }); await adminLog("ERROR_REPORT_RESOLVE","errorReport",item.id,item.errorCode || item.action || "error report","����ς�"); toast("�񍐂����ς݂ɂ��܂����B"); }, "����ς݂ɂ���"));     ['logAction','logSearch','logFrom','logTo'].forEach(id => { const node = $(`#${id}`); if (node) node.oninput = filterLogs; });     [['eventSearch','eventSearch'],['eventStatus','eventStatus'],['artistSearch','artistSearch'],['artistRole','artistRole'],['memberSearch','memberSearch'],['memberStatus','memberStatus']].forEach(([id, key]) => {       const node = $(`#${id}`); if (!node) return;       const apply = () => { state.filters[key] = node.value; render(); };       node.onchange = apply;       if (node.tagName === 'INPUT') node.onkeydown = event => { if (event.key === 'Enter') { event.preventDefault(); apply(); } };     });   }   function filterLogs() { const action = $('#logAction').value, search = $('#logSearch').value.toLowerCase(), from = $('#logFrom').value, to = $('#logTo').value; const filtered = state.data.adminLogs.filter(log => { const date = log.createdAt && log.createdAt.toDate ? log.createdAt.toDate().toISOString().slice(0,10) : ''; return (!action || log.actionType === action) && (!search || String(log.targetLabel || '').toLowerCase().includes(search)) && (!from || date >= from) && (!to || date <= to); }); $('#logTable').innerHTML = table(filtered, ['����','�Ǘ���','����','�Ώ�','�ڍ�'], log => `<tr><td>${timestampText(log.createdAt)}</td><td>${escapeHtml(log.adminDisplayName)}</td><td>${escapeHtml(log.actionType)}</td><td>${escapeHtml(log.targetLabel)}</td><td>${escapeHtml(log.detail)}</td></tr>`); }   async function isAdmin(user) { const [singular, plural] = await Promise.all([state.db.collection('admin').doc(user.uid).get(), state.db.collection('admins').doc(user.uid).get()]); return singular.exists || plural.exists; }   function showSessionWaiting() { $('#loginView').hidden = false; $('#appView').hidden = true; }   function redirectToLogin(reason) {     sessionStorage.setItem('la-admin-exit-reason', reason);     location.assign(ADMIN_LOGIN_URL);   }   function endExpiredSession() {     clearTimeout(sessionTimer);     firebase.auth().signOut().finally(() => redirectToLogin('timeout'));   }   function refreshSessionTimeout() {     if (!state.user) return;     clearTimeout(sessionTimer);     sessionTimer = setTimeout(endExpiredSession, SESSION_TIMEOUT_MS);   }   async function start(user) {     state.user = user;     if (!await isAdmin(user)) { await firebase.auth().signOut(); redirectToLogin('role'); return; }     $('#adminName').textContent = user.displayName || user.email || 'ADMIN'; $('#loginView').hidden = true; $('#appView').hidden = false;     $('#environmentToggle').value = state.environment;     collectionSnapshot('events'); collectionSnapshot('artists'); collectionSnapshot('members'); collectionSnapshot('artistSignals'); collectionSnapshot('errorReports'); collectionSnapshot('adminLogs');     state.unsubscribers.push(state.db.collection('settings').doc('system').onSnapshot(snapshot => { state.settings = { ...state.settings, ...(snapshot.exists ? snapshot.data() : {}) }; const status = $('#systemStatus'); status.textContent = state.settings.systemEnabled === false ? 'SYSTEM: STOP' : 'SYSTEM: ON'; status.classList.toggle('off', state.settings.systemEnabled === false); }));     render();     refreshSessionTimeout();   }   function bindShell() {     $$('.nav-item[data-view]').forEach(button => button.onclick = () => { state.view = button.dataset.view; $('.sidebar').classList.remove('open'); render(); });     ['siteSettingsNav', 'danmakuReviewNav', 'lpDanmakuReviewNav'].forEach(id => {       const button = $(`#${id}`);       if (button) button.addEventListener('click', () => { state.view = null; });     });     $('#operationsButton').onclick = openOperations;     $('#environmentToggle').onchange = event => { state.environment = event.target.value; localStorage.setItem('la-admin-environment', state.environment); toast(`�V�K�f�[�^�̊��� ${state.environment.toUpperCase()} �ɐݒ肵�܂����B`); };     $('#themeButton').onclick = () => { const dark = document.documentElement.dataset.theme !== 'dark'; document.documentElement.dataset.theme = dark ? 'dark' : 'light'; localStorage.setItem('la-admin-theme', dark ? 'dark' : 'light'); $('#themeButton').textContent = dark ? 'Light' : 'Dark'; };     $('#logoutButton').onclick = async () => { clearTimeout(sessionTimer); await firebase.auth().signOut(); if (ADMIN_LOGIN_URL) location.assign(ADMIN_LOGIN_URL); };     $('#menuButton').onclick = () => $('.sidebar').classList.toggle('open');   }   async function init() {     const savedTheme = localStorage.getItem('la-admin-theme'); if (savedTheme === 'dark') { document.documentElement.dataset.theme = 'dark'; $('#themeButton').textContent = 'Light'; }     if (!window.FIREBASE_CONFIG || !FIREBASE_CONFIG.apiKey) { setBanner('Firebase�ݒ肪����܂���Bfirebase-config.js ��m�F���Ă��������B'); return; }     firebase.initializeApp(FIREBASE_CONFIG); state.db = firebase.firestore(); state.storage = firebase.storage(); bindShell();     try { await firebase.auth().setPersistence(firebase.auth.Auth.Persistence.SESSION); }     catch (_) { setBanner('�F�؃Z�b�V������ۑ��ł��܂���Blocalhost �܂��͖{��URL����J���Ă��������B'); }     firebase.auth().onAuthStateChanged(async user => {       state.unsubscribers.forEach(unsubscribe => unsubscribe()); state.unsubscribers = [];       if (!user) { clearTimeout(sessionTimer); state.user = null; showSessionWaiting(); redirectToLogin('auth'); return; }       try { await start(user); } catch (error) { showSessionWaiting(); redirectToLogin('error'); }     });   }   ['pointerdown', 'keydown', 'touchstart'].forEach(eventName => document.addEventListener(eventName, refreshSessionTimeout, { passive: true }));   init(); })();  